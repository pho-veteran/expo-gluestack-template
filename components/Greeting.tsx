import { Text } from "react-native";

const Greeting = () => {
    return (
        <Text
            className="text-center text-lg"
        >
            Hello, World!
        </Text>
    );
}

export default Greeting;